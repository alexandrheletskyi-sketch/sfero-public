
export default function Home() {
  return (
    <main style={{fontFamily:'system-ui', padding:'48px', maxWidth:880, margin:'0 auto'}}>
      <h1>Sfero Public Front</h1>
      <p>Open <code>/PublicProfileSlug?slug=test</code>. In production you'll use <code>/public/:slug</code> on your domain.</p>
    </main>
  )
}
